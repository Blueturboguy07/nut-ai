name: bugfix-lab

on:
  workflow_dispatch:

jobs:
  oracle:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20.19'
      - run: npm install
      - run: pwsh scripts/bugfix-lab/nut-ai-databuild-fdc-foodcsv-missing.ps1
